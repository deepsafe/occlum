[package]
name = "rust_app"
version = "1.0.0"
authors = ["He Sun <bochang.sh@antfin.com>"]
build = "build.rs"
edition = "2021"

[dependencies]
libc = "0.2"
occlum_dcap = { path = "../../../tools/toolchains/dcap_lib" }

[build-dependencies]
cc = "1.0"

